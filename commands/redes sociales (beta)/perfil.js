const Discord = require('discord.js');
const { Message, Client, MessageEmbed } = require('discord.js');
const SchemaPostUser = require('../../models/redes_sociales_user')

module.exports = {
    name: 'perfil',

    /**
     * 
     * @param {Client} client 
     * @param {Message} message 
     * @param {String[0]} args 
     */

    run: async (client, message, args) => {
         
    }
}
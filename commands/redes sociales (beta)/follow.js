const { Client, Message } = require('discord.js');

module.exports = {
    name: 'follow',

    /**
     * 
     * @param {Client} client 
     * @param {Message} message 
     * @param {String[0]} args 
     * @returns 
     */

    run: async(client, message, args) => { 
    }
}
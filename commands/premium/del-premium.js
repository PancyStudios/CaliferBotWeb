const { Client, Message, MessageEmbed } = require('discord.js');
const premium = require('../../models/premium');

module.exports = {
    name: 'rpremium',
    description: 'Quita a un usuario lo premium',
    aliases: ['del-premium'],
    
    /**
     * 
     * @param {Client} client 
     * @param {Message} message 
     * @param {String[0]} args 
     */

    run: async(client, message, args) => { 
    }
}
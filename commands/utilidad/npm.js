 const Discord = require('discord.js');
const fetch = require('node-fetch')

module.exports = {
  name: 'npm',
  aliases: [''],
  category: 'utilidad',
  descripción: 'lose',

 run: async (client, message, args) => { 
 }
}

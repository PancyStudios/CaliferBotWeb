const Discord = require('discord.js');
const PrefixSchema = require('../../models/prefixes')

module.exports = {
	name: 'prefix',
    aliases: ['server-prefix'],
    category: 'utilidad',
	description: 'prefix',

	async run (client, message, args) {
 
	}
}
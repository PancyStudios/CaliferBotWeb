const { Client, Message, Util, MessageEmbed } = require('discord.js');

module.exports = {
    name: 'stealemoji',
    description: 'Añade emojis al servidor', 
    aliases: ['emoji'],
    category: 'utulidad',

    /**
     * 
     * @param {Client} client 
     * @param {Message} message 
     * @param {String[0]} args 
     */

    run: async(client, message, args) => { 
    }
}
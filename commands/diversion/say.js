const Discord = require('discord.js')

module.exports = {
	name: 'say',
	category: 'diversion',
	usage: '<texto>',
	description: 'Hacer que el bot diga algo',

	run: async(client, message, args) => { 
} 
	}
